# This file is sourced every time zsh starts (including sub-shells)
# Is is the first script file read and should be used to set
# environment variables which need to be updated frequently.  Do
# not add aliases!


. ~/.shell/env

logmessage "in ${HOME}/.zsh/env, where \$PATH = ${PATH}"
logmessage "invoking ${HOME}/.zprezto/runcoms/zshenv"
ssource ~/.zprezto/runcoms/zshenv
logmessage "in ${HOME}/.zsh/env, after invoking ${HOME}/.zprezto/runcoms/zshenv, where \$PATH = ${PATH}"


